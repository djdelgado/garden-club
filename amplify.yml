version: 1
applications:
  - frontend:
      buildPath: /
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build --workspace=frontend
      artifacts:
        baseDirectory: frontend/.next
        files:
          - '**/*'
      cache:
        paths:
          - frontend/node_modules/**/*
          - frontend/.next/cache/**/*
    appRoot: frontend